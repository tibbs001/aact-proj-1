default: &default
  adapter: postgresql
  encoding: utf8
  min_messages: warning
  timeout: 5000
  url:  "postgres://tibbs001@localhost:5432/aact_proj"

development:
  <<: *default
  database: aact_proj

test:
  <<: *default
  database: aact_proj_test

production:
  <<: *default
  database: aact_proj
